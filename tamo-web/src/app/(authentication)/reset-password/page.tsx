'use client';
import { FormEvent, Suspense, useState } from 'react';
import { useSearchParams } from 'next/navigation';
function ResetPasswordContent(){const query=useSearchParams();const [message,setMessage]=useState('');async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const password=String(new FormData(event.currentTarget).get('password'));const response=await fetch('/api/auth/reset-password',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({token:query.get('token'),password})});const body=await response.json();setMessage(body.message??body.detail??'Unable to reset password.');}return <main className="authForm authStandalone"><h1>Choose a new password</h1><form className="authForm" onSubmit={submit}><input name="password" type="password" autoComplete="new-password" minLength={12} maxLength={128} placeholder="New password" aria-label="New password" required/><button className="primary">Reset password</button></form>{message&&<p role="status">{message}</p>}</main>}
export default function ResetPassword(){return <Suspense fallback={<main className="authStandalone" aria-busy="true">Loading…</main>}><ResetPasswordContent/></Suspense>}
